import React from 'react';
import ProfilePopup from './ProfilePopupComponent';


const ProfilePopupContainer = (props) => {

  return <ProfilePopup {...props} />
};

export default ProfilePopupContainer;
